{
  "name": "mcp-registry-server",
  "version": "1.0.0",
  "description": "A minimal, in-memory MCP registry server with 3 hardcoded servers",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && cp src/servers.json dist/servers.json",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts",
    "watch": "tsc --watch"
  },
  "keywords": [
    "mcp",
    "registry",
    "model-context-protocol"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/cors": "^2.8.17",
    "@types/node": "^20.10.6",
    "typescript": "^5.3.3",
    "ts-node": "^10.9.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
